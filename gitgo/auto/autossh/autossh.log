
Now try logging into the machine, with "ssh 'root@10.253.252.71'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.72'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.73'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.74'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.75'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.76'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.77'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.78'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.79'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.80'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.81'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.82'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.83'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.84'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.85'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.86'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.87'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.88'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.89'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.90'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.91'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.92'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.93'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.94'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.95'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.96'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.97'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.98'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.99'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.100'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.101'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.102'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.103'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.104'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.105'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.106'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.107'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.108'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.109'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.110'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.111'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.112'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.113'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.114'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.115'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.116'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.117'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.118'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.119'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.253.252.120'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.

expect: spawn id exp4 not open
    while executing
"expect eof"
    (file "./autossh.sh" line 11)
wrong # args: should be "eof channelId"
    while executing
"eof"
    (file "./autossh.sh" line 11)
wrong # args: should be "eof channelId"
    while executing
"eof"
    (file "./autossh.sh" line 11)

Now try logging into the machine, with "ssh 'root@10.0.40.206'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Permission denied, please try again.
root@10.0.40.207's password: 
Now try logging into the machine, with "ssh 'root@10.0.40.192'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.0.40.193'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.0.40.194'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Permission denied, please try again.
root@10.0.40.226's password: 
Now try logging into the machine, with "ssh 'root@10.0.40.226'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.

expect: spawn id exp4 not open
    while executing
"expect eof"
    (file "./autossh.sh" line 11)

Permission denied, please try again.
root@10.0.40.226's password: 
Now try logging into the machine, with "ssh 'root@10.0.40.226'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.0.40.226'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.

expect: spawn id exp4 not open
    while executing
"expect eof"
    (file "./autossh.sh" line 11)

Now try logging into the machine, with "ssh 'root@10.0.40.227'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.0.40.228'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.0.40.229'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.


Now try logging into the machine, with "ssh 'root@10.0.40.230'", and check in:

  .ssh/authorized_keys

to make sure we haven't added extra keys that you weren't expecting.

